# ease-hover-magnetic
Magnetic cursor pull hover effect for EaseMotion CSS.
## Usage
Add `data-magnetic` to any element and include the JS handler in the demo.
## CSS Variables
`--tx`, `--ty` for translation (set by JS).
## Customization
Adjust `magneticStrength` (default 0.4) and `maxDist` (default 120px).
